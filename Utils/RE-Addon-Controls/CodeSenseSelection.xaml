<UserControl x:Class="RE_Addon_Controls.CodeSenseSelection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="300" Loaded="Initialize">
    <Grid>
        <TextBlock x:Name="CompletionTBox" />
        <TextBox Width="280" Height="20" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{Binding ElementName=CompletionTBox,Path=Text,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" DataContext=""  FontSize="12" SpellCheck.IsEnabled="True" Language="en-US"/>
        <Image Width="20" Height="20" HorizontalAlignment="Right" VerticalAlignment="Center" Source="/Utils/RE-Addon-Controls/Enter-Dark.png"  />
    </Grid>
</UserControl>
